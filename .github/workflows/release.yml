name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up JDK 17
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4

      - name: Derive version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Build with release version
        run: ./gradlew build -Pversion="${VERSION}" --no-daemon --warning-mode=all

      - name: Validate plugins
        run: ./gradlew validatePlugins -Pversion="${VERSION}" --no-daemon

      - name: Generate sources and javadoc JARs
        run: ./gradlew sourcesJar groovydocJar -Pversion="${VERSION}" --no-daemon

      - name: Upload all JARs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: plugin-jars
          path: build/libs/*.jar
          retention-days: 90

      - name: Create test reports archive
        run: |
          tar -czf gradle-pitest-plugin-v${VERSION}-reports.tar.gz \
            build/reports/tests/ \
            build/reports/codenarc/ \
            2>/dev/null || true

      - name: Upload reports archive
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: release-reports
          path: gradle-pitest-plugin-*-reports.tar.gz
          retention-days: 90

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: test-reports
          path: |
            build/reports/tests/
            build/reports/codenarc/
          retention-days: 30

  release:
    runs-on: ubuntu-latest
    needs: build-and-test
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Download JARs
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: plugin-jars
          path: artifacts/

      - name: Download reports archive
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: release-reports

      - name: Extract release notes from CHANGELOG.md
        run: |
          VERSION="${GITHUB_REF_NAME#v}"

          # Supports both "## [X.Y.Z]" (Keep a Changelog) and "## X.Y.Z - date" (upstream format)
          awk -v ver="$VERSION" '
            /^## / {
              if (found) exit
              if ($0 ~ ver) { found=1; next }
            }
            found { print }
          ' CHANGELOG.md > release-notes.md

          if [ ! -s release-notes.md ]; then
            echo "No release notes found for version $VERSION in CHANGELOG.md."
            {
              echo "## What's Changed"
              echo ""
              echo "See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/master/CHANGELOG.md) for details."
            } > release-notes.md
          fi

          echo "--- Release notes for $VERSION ---"
          cat release-notes.md

      - name: Create GitHub Release
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "gradle-pitest-plugin ${GITHUB_REF_NAME}" \
            --notes-file release-notes.md \
            artifacts/*.jar \
            gradle-pitest-plugin-*-reports.tar.gz
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
